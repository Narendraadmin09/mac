name: macOS-Remote-Access
on: workflow_dispatch

jobs:
  secure-mac:
    runs-on: macos-latest
    timeout-minutes: 3600

    steps:
    - name: Turn Off Remote Management Conflict
      run: |
        # Remote Management (ARD) ko puri tarah stop aur disable karein taaki Screen Sharing block na ho
        sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -stop -deactivate
        sudo defaults write /Library/Preferences/com.apple.RemoteManagement ARD_AllLocalUsers -bool false

    - name: Create Remote User
      run: |
        USER_NAME="myadmin"
        PASSWORD="MacPass@2026"
        
        # Naya admin user create karein (UniqueID 701)
        sudo dscl . -create /Users/$USER_NAME
        sudo dscl . -create /Users/$USER_NAME UserShell /bin/bash
        sudo dscl . -create /Users/$USER_NAME RealName "Mac User"
        sudo dscl . -create /Users/$USER_NAME UniqueID 701
        sudo dscl . -create /Users/$USER_NAME PrimaryGroupID 20
        sudo dscl . -create /Users/$USER_NAME NFSHomeDirectory /Users/$USER_NAME
        sudo dscl . -passwd /Users/$USER_NAME "$PASSWORD"
        sudo dscl . -append /Groups/admin GroupMembership $USER_NAME
        
        echo "MAC_CREDS=User: $USER_NAME | Password: $PASSWORD" >> $GITHUB_ENV

    - name: Purely Enable Screen Sharing (VNC)
      run: |
        # Screen Sharing service ko turn on aur load karein
        sudo defaults write /var/db/launchd.db/com.apple.launchd/overrides.plist com.apple.screensharing -dict Disabled -bool false
        sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist
        
        # Global VNC configuration files ko directly update karein
        sudo defaults write /Library/Preferences/com.apple.VNCSettings.txt ScreenSharingActive -bool true
        sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCAllowed -bool true
        
        # Firewall ko explicitly band karein taaki network ports block na hon
        sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off

    - name: Install Tailscale
      run: |
        brew install tailscale
        sudo brew services start tailscale

    - name: Establish Tailscale Connection
      env:
        TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
      run: |
        sudo tailscale up --authkey=$TAILSCALE_AUTH_KEY --hostname=gh-mac-runner-$GITHUB_RUN_ID
        
        TS_IP=$(tailscale ip -4)
        if [ -z "$TS_IP" ]; then
          echo "Tailscale IP not assigned. Exiting."
          exit 1
        fi
        echo "TAILSCALE_IP=$TS_IP" >> $GITHUB_ENV

    - name: Verify VNC Accessibility
      run: |
        echo "Tailscale IP: $TAILSCALE_IP"
        # Check karein ki port 5900 response de raha hai ya nahi
        nc -zv -w 5 $TAILSCALE_IP 5900

    - name: Maintain Connection
      run: |
        echo -e "\n=== macOS SCREEN SHARING ACCESS ==="
        echo "Address: $TAILSCALE_IP"
        echo "$MAC_CREDS"
        echo -e "===================================\n"
        
        while true; do
          echo "[$(date)] macOS Remote Active - Cancel workflow to terminate"
          sleep 300
        done
